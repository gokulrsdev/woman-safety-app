# Leave blank for local dev (Vite proxies /api to the local server automatically).
# For production builds, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://mysafeapp-server.onrender.com
VITE_API_URL=
